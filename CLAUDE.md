# 이 저장소에서 일하는 방법

**여러 대화창이 동시에 이 저장소를 고친다.** 창끼리는 서로를 못 본다 —
보이는 것은 `main` 에 올라간 것뿐이다.

## 무조건 먼저

**[`STATUS.md`](STATUS.md) 를 읽고 시작한다.** 지금 무엇이 운영에 나가 있고,
무엇이 테스트에만 있고, 다른 창이 무엇을 잡고 있는지가 거기 있다.

```bash
git pull --rebase origin main     # 이 창의 복사본은 열었을 때 것이다
```

## 무언가를 올릴 때마다

**`STATUS.md` 의 「기록」에 한 줄 넣는다 — 변경과 같은 커밋에.**
나중에 적기로 하면 안 적힌다. 날짜 · 커밋 · 무엇이 · 어디까지(테스트/운영).

승격했거나, 운영에 나가는 것이 달라졌으면 **「지금 운영에 나가 있는 것」**
표도 같이 고친다.

## 커밋 안 한 것은 없는 것이다

한 덩어리가 끝날 때마다 올린다. 하루 끝이 아니라. 한 창이 몇 시간 들고 있던
작업 때문에 하마터면 남의 작업을 소리 없이 되돌릴 뻔한 적이 있다.

```bash
git pull --rebase origin main
node plate3d/tools/check_....js   # 건드린 것에 딸린 검사
git push origin HEAD:main
```

`main` 하나로 간다. 브랜치를 늘리지 않는다.

## 조용히 덮이는 경우는 딱 하나

**파일 통째 복사(승격)** 다. 내용을 합치지 않고 갈아 끼우므로 내 복사본에 없는
남의 작업이 충돌 한 번 없이 사라진다.

- **승격은 한 창에서만.** 시작 전에 `STATUS.md` 의 「지금 누가 무엇을 잡고
  있나」에 적고, 끝나면 지운다
- 승격 직전 `git diff --stat origin/main -- <테스트 파일>` 이 비어 있어야 한다
- **`--force` 는 쓰지 않는다.** 거절당했으면 `pull --rebase` 하고 다시 올린다

그 밖의 충돌은 `git` 이 알아서 하거나 충돌로 세워 준다. **충돌은 사고가 아니라
알림이다.**

## 자세한 규칙

| | |
|---|---|
| 지금 어디까지 갔나 · 창마다 매번 할 것 | [`STATUS.md`](STATUS.md) |
| PLATE3D 전반 · 테스트본/운영본 분리 · 포맷 잠금 | [`plate3d/README.md`](plate3d/README.md) |
| 시트 문법 | [`plate3d/DATA_SCHEMA.md`](plate3d/DATA_SCHEMA.md) |
| 테스트 → 운영 동기화 | `design/SYNC.md` (다른 저장소) |
