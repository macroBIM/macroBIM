# STATUS — 지금 무엇이 어디까지 가 있나

**여러 창에서 같이 일한다. 이 파일이 그 창들이 공유하는 유일한 현재 상태다.**

창을 열면 여기부터 읽고, 무언가를 올릴 때마다 여기를 같이 고친다.
**변경과 같은 커밋에 넣는다** — 나중에 적기로 하면 안 적힌다.

> 오늘 실제로 어긋난 자리는 하나였다. **커밋 안 된 작업은 다른 창에 안 보인다.**
> 한 창이 몇 시간 들고 있던 작업이 그동안 `main` 이 앞서 가는 줄 몰랐고,
> 그 사이에 만든 승격 파일은 이미 옛것이었다. 하마터면 남의 작업을 소리 없이
> 되돌릴 뻔했다. 문서로는 이게 안 고쳐진다 — 창 A 에서 쓴 문서도 push 전에는
> 창 B 가 못 본다. **작게 자주 올리는 것**이 고치는 방법이고, 이 파일은 그렇게
> 올라온 것들이 한자리에 모이는 곳이다.

---

## 창마다 매번 할 것

**① 시작할 때 — 무조건 먼저**

```bash
git pull --rebase origin main
```

창을 오래 열어 두었으면 그 컨테이너의 복사본은 그때 것이다. 컨테이너가
도중에 새로 클론되면서 브랜치 이름이 바뀌어 있던 적도 있다.

**② 한 덩어리가 끝날 때마다 커밋·푸시** — 하루 끝이 아니라

```bash
git pull --rebase origin main     # 그 사이 다른 창이 올렸을 수 있다
node tools/check_....js           # 건드린 것에 딸린 검사
git push origin HEAD:main
```

**커밋 안 한 것은 없는 것이다.**

**3D 모듈을 고쳤으면 `?v=` 를 같이 올린다.** 페이지는 새로 받아도 3D 모듈은
주소가 같으면 브라우저가 옛 파일을 그대로 쓴다 — 화면이 안 바뀌고, 새로 넣은
함수를 못 찾아 버튼이 조용히 먹통이 된다. 고친 줄 알고 한참 헤매게 된다.

| 3D 파일 | 올릴 곳 |
|---|---|
| `bim_pscboxdia_3d.js` | `bim_pscbox_diaphragm_test.js` 의 `V3D` |

**③ 승격할 때 — 여기가 유일하게 위험한 자리**

```bash
git pull --rebase origin main
git diff --stat origin/main -- plate3d/plate_builder_test.js   # 반드시 비어야 한다
cp plate3d/plate_builder_test.js plate3d/plate_builder.js
# plate3d/embed.html 과 design/layout_body.js 의 ?v= 를 둘 다 올린다
node tools/lock_format.js            # 먼저 그냥 돌려 무엇이 달라졌나 본다
node tools/lock_format.js --update   # 이유를 커밋에 쓸 수 있을 때만
```

승격은 **파일 통째 복사**라, 내 복사본에 없는 남의 엔진 작업을 소리 없이
되돌린다. 그래서 **승격은 한 창에서만** 한다. 시작하기 전에 아래
「지금 누가 무엇을 잡고 있나」에 적고, 끝나면 지운다.

**④ 끝낼 때**

```bash
git status --short     # 비어 있어야 한다
```

---

## 두 창이 동시에 올리면

대부분은 **git 이 알아서 한다.** 오늘도 PLATE3D 창과 PSCBOX 창이 나란히
올렸지만 서로 다른 파일이라 한 번도 안 부딪쳤다.

| 무엇 | 어떻게 되나 | 할 일 |
|---|---|---|
| **다른 파일** | 저절로 합쳐진다 | 없음 |
| **같은 파일, 다른 자리** | 저절로 합쳐진다 | 없음 |
| **같은 파일, 같은 자리** | `git pull --rebase` 가 **충돌로 세운다** | 손으로 고른다 |
| **동시에 push** | 나중 쪽이 거절당한다 (non-fast-forward) | `git pull --rebase` 하고 다시 push |

**충돌은 사고가 아니라 알림이다.** 무서운 건 충돌 없이 조용히 덮이는 쪽이다.

### 조용히 덮이는 경우는 딱 하나 — 파일 통째 복사

승격(`cp ..._test.js → ....js`)은 내용을 합치지 않고 **통째로 갈아 끼운다.**
내 복사본에 없는 남의 작업이 충돌 한 번 없이 사라진다. 그래서:

- **승격은 한 창에서만.** 시작 전에 위 「지금 누가 무엇을 잡고 있나」에 적는다
- 승격 직전에 `git diff --stat origin/main -- <테스트 파일>` 이 **비어 있어야** 한다
- **`--force` 는 쓰지 않는다.** 거절당했으면 `pull --rebase` 하고 다시 올린다

### 이 파일이 충돌하면

「기록」 은 맨 위에 넣으므로 두 창이 같은 자리를 건드리기 쉽다.
**둘 다 남기면 된다** — 골라 버릴 일이 아니다. 날짜순으로 붙여 놓고 끝낸다.

---

## 지금 운영에 나가 있는 것

| | |
|---|---|
| 엔진 | `plate3d/plate_builder.js` · **`?v=94`** |
| 승격한 커밋 | `b7635e1` (2026-09-14) — 예제 워크북 + Example 문구 |
| 짝이 되는 design 커밋 | `?` — `layout_body.js` 의 `embed.html?v=94` |
| 테스트 엔진과의 차이 | **없음** — 승격 직후라 두 파일이 같다 |

`?v=` 는 **두 군데**다. `plate3d/embed.html` 의 `plate_builder.js?v=` 와
`design/layout_body.js` 의 `embed.html?v=`. 한쪽만 올리면 브라우저가 캐시에서
옛 `embed.html` 을 꺼내고 그게 옛 엔진을 부른다 — 엔진을 올려도 화면에는 안
나온다. 한 번 밟은 구멍이다.

`design/layout_body.js` 의 `quick_simpleconn.js?v=` 는 **저 쌍에 딸린 것이 아니다.**
그 파일 하나만 가리키므로 그 파일이 바뀌었을 때만 올린다.

---

## 테스트에만 있는 것 — 아직 운영에 없다

| | 어디 | 왜 |
|---|---|---|
| **PSCBOX 격벽** | `bim_pscbox_diaphragm_test.js` | 개구부는 끝났다 — 입력·그림·엑셀·벽 고리. 남은 것은 **철근**이다 (아래 「아직 안 한 것」) |
| **Crossbeam 폼** | `plate3d/quick_crossbeam_test.js` | 폼과 그림은 있고, PLATE3D 로 모델 보내기와 워크북 내보내기가 없다 |
| **Dashboard · PSCBOX · Crossbeam 메뉴** | `design/layout_body_test.js` | `design/SYNC.md` 참조 — 옮기지 않는다 |

---

## 지금 누가 무엇을 잡고 있나

> 승격처럼 **파일을 통째로 바꾸는 일**을 시작하기 전에 여기 적고, 끝나면 지운다.
> 창끼리 안 부딪치게 하는 것이 목적이지 일지가 아니다.

- (없음)

---

## 아직 안 한 것 — 정해 놓고 미룬 것

| | 어디에 적혀 있나 |
|---|---|
| **테스트 페이지가 `main` 이 아닌 브랜치를 보게 하기** — 검증하려고 올리는 것이 검증 전에 공개된다 | `plate3d/README.md` 「테스트본 / 운영본 분리」 |
| **철근 절곡 · 곡선 경로 sweep** — 직선 변단면으로 구조물을 먼저 만들어 보고 정한다 | `plate3d/DATA_SCHEMA.md` TAPER |
| **재질별 비중** — `RHO` 가 강재로 고정이라 슬래브·방호벽도 강재로 잰다 | — |
| **격벽 철근을 올릴 때 셀 내면을 인력 경계에서 뺄지** — 격벽 평면에서는 셀이 콘크리트로 차 있으므로 「외곽 + 개구부」만 남는 것이 맞다. 지금은 박스 단면의 `inner` 벽이 그대로 들어 있다 | 이 파일 · 2026-09-11 `c69d3aa` |
| **데크 철근 `1`·`1-1` 의 끝 190 다리가 슬래브 밖으로 솟는다** — 42 점 중 15 점이 외곽 바깥. 꺾임각 부호도 `nors` 도 다리는 돌리지만 철근을 벽 따라 미끄러뜨린다. 다리를 **선단에 둔 채** 안쪽으로 돌리는 입력을 찾아야 한다 (`barEnds` 의 `end:start`/`end:end` 로 보인다). 확인은 `tools/rebar_probe.js` | 이 파일 · 2026-09-15 |
| **격벽 덕트 구멍** — 개구부와 같은 자리(벽 등록 · 3D 구멍 · 격벽면 뷰)에 넣으면 되지만, 덕트는 철근이 **붙을 면이 아니라 피해야 할 장애물**이다. 지금 엔진에는 인력만 있고 배제가 없어 그냥 넣으면 철근이 덕트에 달라붙는다. 그래서 `_ducts` 로 따로 두고 벽 태그도 `'duct'` 로 나눠야 한다. 원형은 16~24각형 근사(48 은 벽이 너무 는다), 입력은 엑셀 `duct` 여러 줄, 3D 는 우선 수직 관통만. **정할 것 : 구멍만 뚫을지, 둘레에 철근을 붙일지** | 이 파일 · 2026-09-15 |

---

## 기록

**맨 위가 최신.** 한 줄에 하나: 날짜 · 커밋 · 무엇이 · 어디까지(테스트/운영).

### 2026-09-15

- **교축방향 폐합철근 ⑩ ⑩-1 ⑩-2 — 평면은 되고, 닫힘만 안 된다** · 테스트 —
  `PSCBOX_DIAPHRAGM_S15.xlsx` 에 `crebar` 블록을 넣고, 실제로 되는지
  `tools/plan_hoop_test.js` 로 **엔진을 그대로 돌려 봤다.**

  **세운 생각** : 물리는 **2D 평면 하나**만 안다 — 그 평면이 단면(x‑y)이어야 할
  이유가 없다. 격벽을 높이 y 에서 수평으로 자르면 또 하나의 평면이 나오고, 그
  평면의 콘크리트는 「그 높이의 단면 폭 × 격벽 두께」인 직사각형이다. 네 면을
  벽으로 주면 지금 인력장이 그대로 돈다. 가로는 x, 세로는 교축 z 로 뜻만 바뀐다.
  **그래서 폭을 입력하지 않는다 — 높이만 주면 폭은 콘크리트가 정한다.**

  **결과 ①** 교축방향 평면은 **문제가 없다.** 직선(code 1) · ㄱ자(11) · U자(21)
  를 떨어뜨리면 전부 `FORMED` 이고, 벽에서 **52 = 피복 40 + 지름반 12.5** 로
  정확히 선다. U자를 두께를 감는 크기로 주면 앞뒤 두 면과 옆면을 함께 문다
  (z −948 ~ 953, 벽은 ∓1,000) — **고리에서 한 변만 빠진 모양**이다.

  **결과 ②** 막히는 것은 **닫힘 하나뿐**이다. `code 41`(다섯 조각)은 치수를
  어떻게 주든, `set` 을 주든 말든 `ASSEMBLING` 에서 멈춘다. 엔진에 닫힌 형상이
  없다는 것이 숫자로 확인됐다. → **U자(21) + 닫는 한 변**으로 가는 길이 열려 있다.

  **부수 확인** 도면의 랩 포함 길이가 격벽 두께 2,000 에서 정수로 떨어진다 :
  ⑩ 11,624 = 2×(3,812+2,000) · ⑩‑1 12,624 = 2×(4,312+2,000) ·
  ⑩‑2 27,124 = 2×(11,562+2,000). 셋 다 맞는다 — **폐합철근이 격벽 두께를
  감는다는 것과 두께 2,000 의 뒷받침**이다. 높이(`at`)는 이 폭에서 거꾸로 짚은
  가정이라 주두부 배근도 (3)(4)(5) 로 확인해야 한다.

- **3D 철근 형상 — 원인을 잡았다 (엔진을 브라우저 없이 돌리는 하네스)** · 테스트
  — 3D 철근이 이상하다는 지적. 눈으로 못 보니 `tools/rebar_probe.js` 를 만들어
  **엔진을 node 에서 그대로 돌렸다.** `physics.js` · `trebar.js` 는 브라우저
  의존이 없고 `domain.js` 도 거의 없다. Konva 가 필요한 것은 `UI` 뿐이라
  UI 를 건너뛰고 `Domain.stepPhysics()` 를 직접 돈다. 단면·치수·피복·철근은
  전부 페이지의 로더(`_loadDimsFromExcel` / `_parseRebar` / `_buildSectionFromBim`)
  를 태워 화면과 같은 길을 가게 했다. 이제 철근 좌표를 숫자로 볼 수 있다.

  **찾은 것 ①** 데크 철근 `1` · `1-1` 의 끝 190 다리가 **슬래브 밖으로 솟는다** —
  외곽 폴리곤 안팎 판정으로 42 점 중 15 점이 바깥이다. 2D 에도 똑같이 있다.
  3D 코드 탓이 아니라 배치/입력 쪽이다. 꺾임각 부호를 뒤집으면 다리는 안으로
  들어가지만 철근이 벽을 따라 미끄러지고(끝이 캔틸레버 선단에서 x −3,037 로),
  `nors` 로 뒤집어도 마찬가지다. **아직 못 고쳤다** — 다리를 선단에 둔 채
  안쪽으로 돌리는 입력이 무엇인지 (`barEnds` 의 `end:start`/`end:end` 로 보인다)
  더 봐야 한다.

  **찾은 것 ②** 벽 id(`E1`,`E2`…)는 **단면이 바뀌면 다시 매겨진다.** S15 파일을
  안 읽은 기본 단면에 S15 철근 행을 얹으면 벽이 24 개가 아니라 40~60 개가 되고,
  다리 하나가 반대편 벽에 붙는다. 길이 1,590 인 철근 `2` 가 **13,516** 으로,
  세그먼트 사이가 **12,126 mm** 벌어진 채 나왔다 — 단면을 가로지르는 유령 철근.
  철근 행은 **그 행을 쓴 단면에서만** 유효하다.

  **고친 것 — 갈고리가 폐합 삼각형으로 보이던 것.** 캔틸레버 선단 ㄴ자 다리가
  갈고리가 아니라 닫힌 삼각형처럼 보였다. `TubeGeometry` 는 곡선을 따라가며
  단면 방향(Frenet frame)을 이어 붙이는데, **90° 가까운 꺾임에서 그 방향이 홱
  뒤집히면서 튜브가 꼬인다.** `CatmullRomCurve3` → 선분(`LineCurve3`)으로 바꿔도
  그대로였다 — 꺾임 자체가 원인이기 때문이다. 이제 **구간마다 원기둥을 따로
  세우고 꺾이는 자리에 구를 하나 둔다.** 프레임이 구간을 넘어 이어지지 않으니
  꼬일 일이 없고, 구가 이음매를 메운다. (스텁 확인 : ㄴ자 한 가닥 = 원기둥 2 +
  구 1, 두께 600·ctc 150 이면 4 겹으로 원기둥 8 + 구 4.)
  선(Line)은 처음부터 원래 점을 그대로 이었다 — 손대지 않았다.

  **거르개는 넣었다가 되돌렸다.** 미안착·끊긴 철근을 3D 에서 빼 봤는데,
  **3D 는 배치의 심판이 아니라 2D 의 같은 좌표를 교축으로 펴는 화면이다.**
  2D 는 미안착 바를 일부러 직선으로 남겨 두는데(`_finalizeArcs`) 3D 만 빼면
  철근이 사라진 것처럼 보인다. 2D 에 보이는 것은 3D 에도 보여야 한다.
  끊긴 철근은 콘솔에만 남기고 그리기는 2D 와 똑같이 이어 그린다.
  ①·② 는 3D 결함이 아니라 배치/입력 쪽 사실이다 — 3D 가 가릴 일이 아니다.

- **화면에서 확인됐다** · 테스트 — 아래 2026-09-15 의 격벽 3D 항목들은 전부
  "화면은 못 봤다" 로 적어 두었는데, 오늘 화면에서 **확인됐다.** 격벽 한 장이
  개구부와 함께 뜨고, 데크 슬로프가 살아 있고, 시점·투영 버튼이 동작한다.
  (내 환경은 cdnjs·unpkg 가 막혀 three.js 를 못 받는다. 그래서 3D 는 스텁으로
  숫자만 확인하고 화면은 늘 사용자 확인을 받는다 — 앞으로도 그렇다.)

- **3D 모듈에 버전 쿼리(`?v=`)** · 테스트 — 투영법 버튼을 올린 뒤 화면이 한동안
  안 바뀌었다. 페이지는 새로 받는데 3D 모듈(`bim_pscboxdia_3d.js`)은 주소가
  같아 브라우저가 옛 파일을 그대로 썼기 때문이다. 옛 파일에는 `setProjection`
  이 없으니 버튼도 먹통이었다 — "ISO 가 여전히 작다" 와 "투영 버튼이 안 먹는다"
  가 같은 원인이었다. 집의 다른 곳(`bim_pier_test.js`)처럼 `?v=` 를 붙였다.
  **3D 파일을 고칠 때마다 `V3D` 를 올린다.** 지금 2.
  규칙은 맨 위 「창마다 매번 할 것 ②」 에도 적어 두었다.

- **투영법 버튼(PERSP / ORTHO) · 시점 거리를 정확히** · 테스트 — 정면도에서
  뒷면이 작아 보인 것은 투시 투영이라 맞다. 이제 **정사(ORTHO)** 를 고를 수
  있다 — 거리와 크기가 무관하므로 앞뒤 면이 같은 크기로 겹쳐 도면과 같은
  그림이 된다. 카메라 객체가 바뀌므로 OrbitControls 를 다시 만들되 보던
  각도·타깃은 그대로 잇는다.
  **거리 계산을 어림에서 정확한 값으로 바꿨다.** 예전에는 "가장 큰 반높이 / t
  + 가장 큰 반두께" 로 더했는데, 두 최댓값이 서로 다른 꼭짓점에서 나오는 것을
  같은 점인 양 더해서 카메라가 필요 이상으로 물러났다. ISO 에서 특히 심했다.
  이제 바운딩 박스 여덟 꼭짓점마다 `D >= p·dir + |p·up|/t` (가로도 같은 식)
  을 풀어 최댓값을 취한다 — 정확한 최소 거리다. 정사는 같은 방식으로 틀의
  반높이를 구한다. 여유는 3 %.
  검증 : 12.4 × 7.8 × 0.6 m 기준 iso·front·left·top 이 모두 **화면 97 %** 를
  채운다 (100 % = 딱 맞음). ISO 거리 21.2 → 15.2 m 로 줄어 구조물이 약 39 %
  커졌다. ORTHO ↔ PERSP 왕복도 스텁으로 확인했다.

- **3D 에 데크 슬로프가 없던 것 · 시점 버튼** · 테스트 — 두 가지다.
  (1) **슬로프** : `_collect3D()` 가 외곽을 점 이름으로 손수 나열하고 있었다
  (`PTL → PTR → …`). 크라운 `PTC` 가 빠져 상면이 직선 하나로 눌렸고, 그래서
  좌우 슬로프(SLL/SLR)를 넣어도 3D 상판이 평평했다. 헌치·캔틸레버 끝도 같은
  이유로 빠져 있었다. 이제 **2D 물리가 쓰는 그 고리를 그대로 쓴다** —
  `_buildSectionFromBim()` 이 체이닝한 외곽·셀 고리를 `_sectPoly` 에 담아
  넘긴다(점 이름 나열은 단면을 세우기 전의 예비 경로로만 남겼다).
  단면을 만들고 밀어내는 방식 자체는 PLATE3D 와 같다 — 틀린 것은 그 "단면" 이었다.
  (2) **시점 버튼** : 3D 칸 오른쪽 위에 ISO / FRONT / BACK / TOP / BOTTOM /
  LEFT / RIGHT. 화면 위에 얹어 뷰 넓이를 뺏지 않는다. 장면이 이미 서 있으면
  다시 세우지 않고 카메라만 옮긴다(`state.setView`). 거리는 그 시점에서
  화면 가로·세로에 놓이는 치수를 화각에 맞춰 계산한다 — 단면이 꽉 찬다.
  FRONT 가 격벽면(단면) 정면이고 TOP 은 상판을 내려다본다.
  검증 : 12.4 × 7.8 × 0.6 m 기준으로 front/left 14.5 m · top 13.3 m · iso 21.2 m,
  up 벡터가 top 에서 (0,0,−1) 로 바뀌는 것까지 스텁으로 확인했다.

- **3D 가 버튼을 눌러야 나오던 것** · 테스트 — `refresh3D()` 를 부르는 곳이
  `_finalizeArcs()` 뿐이었다. 그래서 철근이 안착해야 처음 그려졌고, 철근이
  없으면 버튼을 누르기 전까지 빈 칸이었다. 이제 `redraw()` 끝에서도 부른다 —
  콘크리트는 바로 서고 철근은 안착 뒤 다시 갱신된다. 입력칸을 두드릴 때마다
  장면을 새로 세우지 않도록 150 ms 모았다 한 번만 부르고(`_refresh3DSoon`),
  three.js 를 받는 중이면 겹쳐 부르지 않고 500 ms 뒤 다시 시도한다.
  **카메라를 기억한다** — 다시 그릴 때 보던 각도로 되돌려 준다(`keep`).
  **격벽구간 길이 기본값 600 → 2,000 mm** (`DIA_DEF`), 입력칸 이름도
  `Diaphragm Length (mm)` 로 바꿨다.

- **3D 는 격벽 한 장만, 2D·3D 를 나란히** · 테스트 — 두 가지를 고쳤다.
  (1) **일반구간이 같이 나오던 것** : 처음엔 셀을 뚫고 세그먼트 길이 18,000 으로
  밀어 일반구간 박스가 나왔다. 입력한 것은 격벽 두께인데 일반구간이 같이 나오는
  것이 맞지 않다. 이제 **외곽을 격벽 두께만큼만** 밀고 **개구부만 구멍**으로 둔다 —
  격벽에서는 셀이 콘크리트로 차 있으므로 셀을 뚫지 않고, 셀 자리는 앞뒤 면에
  얇은 선으로만 남긴다. 철근도 격벽 두께 안에서만 선다 (가로 `n = round(t/ctc)` 겹,
  세로 ∓t/2). (2) **배치** : `.phys-split` 이 2D | 3D 반반인 한 줄이 되고
  **철근 상세 표는 그 아래 전폭**으로 내려갔다. 표 높이를 2D 뷰에 맞추던 두
  군데(`_fitEngineStage`·`_renderPhysicsTable`)는 지웠다 — 이제 320 px 고정이다.
  3D 는 켠 채로 시작하고(`_is3D: true`), 버튼은 3D 칸을 끄고 켜는 스위치다.
  검증 : 1,400 px 폭에서 2D 700 · 3D 700 · 표 1,400 (headless Chromium 실측),
  3D 를 끄면 2D 가 전폭이 되며 460 px 에서 높이가 묶인다. THREE 를 흉내 낸
  스텁으로 압출이 **한 번, 깊이 0.6 m(= 두께 600), 구멍 1개**, 철근 z 가
  −225 · −75 · 75 · 225 (150 간격, 가운데 정렬) 임을 확인했다.
  **화면 자체는 여전히 못 봤다** — cdnjs·unpkg 가 막혀 three.js 를 못 받는다.

- **격벽 세그먼트 3D 뷰** · 테스트 — `bim_pscboxdia_3d.js` (새 파일) + 격벽 페이지
  Rebar Physics 카드에 **2D / 3D 토글**. 집의 다른 3D 와 같은 경로다 —
  `RWSVG.render3d()` 가 three.js(cdnjs r128)와 OrbitControls 를 받아 전역
  `render_pscboxdia_3d(hostId, cfg)` 를 부른다 (`bim_box1cell_3d.js` 와 같은 자리).
  **물리는 2D 그대로다.** 격벽이 평면 구조물이라 인력장은 단면 하나에서 돌고,
  3D 는 그 결과 좌표를 교축(z)으로 펼 뿐이다 — 철근을 3D 로 다시 풀지 않는다.
  3D 파일은 엔진을 모른다. 페이지가 평범한 배열(outer/cells/openings/trebar/
  lrebar)로 넘긴다. 안착이 끝난 뒤(`_finalizeArcs`)에만 갱신한다.
  **격벽 두께 입력칸을 새로 만들었다** (`diaThk_s`, 기본 600) — 도면에 치수가
  없어 그동안 가정으로만 쓰던 값이다. 세그먼트 길이 옆에 둔다.
  검증 : 토글·호스트·입력칸 존재, `_collect3D()` 가 1 Cell [셀 14점 · 개구부 8점],
  2 Cell [셀 12+12 · 개구부 8+8], segLen 18,000 · diaT 600 을 넘긴다.
  **화면은 확인 못 했다** — 이 환경은 cdnjs·unpkg 가 막혀 three.js 를 못 받는다.

### 2026-09-14

- `b7635e1` **예제 워크북에 TAPER — `?v=94`** · **운영**
  가이드(§6.6)는 승격 때 같이 갔는데 **예제 15권 어디에도 TAPER 가 없었다.**
  - `PLATE3D_BASIC.xlsx` — 시트 제목이 "every keyword, one small model" 인데
    빠져 있었다. 있는 형상은 안 건드리고 **더했다**: 기둥 바깥면에 매다는
    변단면 브래킷(끝 H110 → 기둥 쪽 H200, `tc tc` 로 윗면 평평). `md.col` 에
    넣어 MIR·COPY 를 타고 여섯이 된다 — 변단면이 대칭·복사를 지나가는 것까지
    한 번에 보인다. 처음엔 클리트 판과 10 mm 겹쳐서 판 바깥면(x −610)에서
    멈추게 물렸다. 90 부재 1.70 t → **96 부재 1.81 t**, 겹침 0
  - `PLATE3D_TEMPLATE.xlsx` — "모든 키워드와 형식"이라면서 `TAPER`·`NOTCH`·
    `PLOT` 이 없었다. 셋을 채웠다. **`POS` 는 안 넣는다** — 가이드에 한 번도
    안 나오는 옛 형식이라 템플릿이 가르칠 것이 아니다. `VIEW` 줄이 실제
    문법과 달랐던 것(scale 칸 누락, 방향 목록 부족)도 같이 고쳤다.
    여전히 0 부재 0 오류
  - Example 목록 문구를 실제 값으로: Basic `82 rows → 96 members · 1.81 t`,
    Template `179 rows`
  포맷 잠금 **변화 없음** — BASIC 의 BOQ 모양이 그대로다.
  회귀 아홉 종 전부 통과 (samples 15권 · taper 34 · notch 34 · by 20 ·
  view3d_dxf 33 · clash 15 · tutorial 62 · column ✓ · simpleconn 11)

### 2026-09-11

- `dbed4bb` **격벽 개구부 — X 지시선을 단면 위로** · 테스트
  셀 안은 해치에 B·H·Y 까지 겹쳐 좁다. 상판 위 빈자리로 뺐고 중심선이 그
  선까지 올라간다 (라벨 y 103, 상판 윗면 123)
- `929c728` **2 Cell 이면 같은 개구부를 좌·우 셀에 하나씩** · 테스트
  치수는 한 벌이고 자리만 셀 수를 따른다 — 1 Cell 은 X 에 하나, 2 Cell 은
  ∓|X| 로 둘. 좌·우 X 지시선의 글자 높이가 달랐던 것은 치수선 방향 탓이었다
  (렌더러가 라벨을 선 반대쪽에 놓는다) — 항상 왼→오른쪽으로 긋는다. 148/124 → 121/121
- `0ae366a` **개구부는 하나 · 입력칸과 그림을 각각 1/3 폭으로** · 테스트
  Cell 2 패널을 없앴다. 엑셀 `open` 줄에서도 셀 번호를 뺐고, 형상 자리에 숫자가
  오면 옛 형식으로 보고 한 칸 건너뛴다 — 숫자를 형상으로 잘못 읽어 조용히
  기본값이 되는 일이 없게. `PSCBOX_DIAPHRAGM_S15.xlsx` 도 새 형식으로 고쳤다
- `c69d3aa` **개구부 콘크리트 면을 닫힌 벽 고리로 — 인력장이 붙을 자리** · 테스트
  `splitWallLoops` 가 **배열 순서**로 고리를 끊으므로(마지막 끝점이 첫 시작점과
  만나면 한 고리) 개구부를 한 덩어리로 연달아 넣고 마지막이 첫 점으로 돌아오게
  했다. 반시계로 통일해 법선을 구멍 바깥(콘크리트 쪽)으로 모으고 `tag` 는 `inner`.
  **`inside()` 에는 넣지 않는다** — 경계에 더하면 홀·짝이 뒤집혀 단면 루프의 법선이
  망가진다. 검증: 연속·연결·폐합, 법선 안쪽 0개, 피복 오프셋이 구멍을 넓힘
  (800→860 · 1200→1260, Interior 30)
- `67e5c12` **격벽면 뷰 높이를 입력 카드에 맞춘다** · 테스트
  치수 간격을 단면 크기(S)에 비례시킨 탓에 얕은 단면에서 X 치수선이 상판 위로
  한참 올라가 그림 범위를 부풀리고 있었다 — 개구부 크기 기준으로 바꿨다
- `6a191ff` **개구부 기본 크기 800 × 1000** · 테스트 (모서리 접기도 같은 비율로)
- `997451d` **2 Cell 로 바꾸면 개구부가 셀 한가운데로** · 테스트
  그때는 셀마다 입력칸이 따로 있었다. `0ae366a` 에서 입력칸을 하나로 줄이며
  같이 걷혔다가, `929c728` 에서 「값 하나 · 자리 둘」로 다시 들어왔다
- `432ccd0` **OPENING 카드에 격벽면 뷰** · 테스트
  값만 받고 어디에 뚫리는지 안 보여 주고 있었다. 시안 셋 중 「격벽면 전용 뷰」.
  단면 가이드(공용 `draw_box12cell_guide`)는 건드리지 않았다 — 거기에 치수를 더
  얹어 보니 `Y` 가 `TBS`·`TBHL2` 라벨 위에 겹쳤다 (이미 65개가 붙어 있는 그림이다)
- `d164120` **엑셀에 `seg`·`open` 블록** · 테스트
  8-020 단면 A-A 의 개구부(팔각 1,200 × 1,500, 모따기 250×250, 중앙, 슬래브
  상면에서 700)를 파일이 싣는다. 세로 치수 4.800 + 1.500 + 1.500 = 7.800 = Y1
  로 닫히는 것으로 위치를 확정했고, 8점이 PDF 벡터 실측과 일치
- **`CLAUDE.md` 두 벌** · 문서 — macroBIM `0ed3ee4` · design `5da6b73`
  창이 열리면 저절로 읽히므로 「`STATUS.md` 부터 읽고, 올릴 때 기록에 한 줄」을
  그 자리에 박았다. **저장소가 둘이라 파일도 둘**이다 — `design` 만 열린 창은
  `macroBIM/CLAUDE.md` 를 못 본다. 대신 겹치는 말은 없앴다: 규칙은 macroBIM
  쪽 한 벌이고, design 쪽은 가리키는 줄과 그 저장소만의 함정 둘만 남긴다

### 2026-09-10

- `ef59769` **`STATUS.md` — 이 파일** · 문서
  `a241f98` 과 나란히 올라갔는데 서로 다른 파일이라 **충돌 없이 합쳐졌다.**
  위 「두 창이 동시에 올리면」 이 실제로 한 번 돈 자리다
- `a241f98` **PSCBOX — S15 교각부 단면 입력파일** · 테스트 (다른 창)
- `ada0605` **운영 승격 — TAPER (`?v=93`)** · **운영**
  변단면 부재가 운영으로 나갔다. 포맷 잠금을 다시 떴다(키워드 표 24→25) —
  차이 104개가 전부 「표 하나가 끼어들며 뒤가 한 칸씩 밀린 것」임을 집합으로
  확인했다: 사라진 표 0, 새 표 1. 부재표 워크북 시트·열과 치수 상수 15개,
  DXF 레이어는 차이 0. 기존 시트는 `TAPER` 행을 안 쓰므로 영향 없다.
  짝: design `7b748e9`
- `8f3ccf5` **`tools/check_taper.js` — 34가지 · TAPER 문서 동기화** · 테스트
  숫자를 박지 않고 「반드시 참인 문장」으로. 엔진 문법 오류도 하나 고쳤다
  (`a H section` → `an H section`). README · CUTTING · design/SYNC 갱신
- `121f108` **가이드에 TAPER 장 (§6.6)** · 테스트
- `07c167b` **형상이 다른 변단면을 거절 · 속이 빈 단면의 구멍이 따라 흐른다** · 테스트
  각형강관→통짜 판이 꼭짓점 개수만 같아 통과하고 있었다. 그리고 변단면
  강관의 구멍이 사라져 877 kg 짜리가 5145 kg 으로 나오고 있었다. 고친 뒤에도
  605 kg 이 272 kg 이었는데, 시작 링을 잘라낸 결과에서 가져와 `PolyBool` 이
  링을 다시 짜 놓은 탓이었다 — 이제 두 링을 같은 생성기에서 뽑는다
- `c2e519c` **TAPER 는 길이 한 칸으로 끝난다 · 부재표는 두 단면을 적는다** · 테스트
  `from`/`to` 를 뺐다. 부재표에서 변단면이 `NOTCH kg` 공제 열을 쓰던 것을
  끝단면 줄로 바꿨다 — 파낸 적 없는 노치를 적는 것은 숫자가 맞아도 거짓말이다
- `7f17105` **TAPER 는 부재를 고치는 게 아니라 만든다** · 테스트
- `c27571d` **TAPER — 변단면 부재** (첫 판) · 테스트
- `fd434ea` **둥근 부재의 테두리를 호로 그린다** — 원판이 직선 48개였다 · 테스트
  → `ada0605` 로 운영에 같이 갔다

### 그 전

`git log --oneline` 으로 본다. 이 파일은 **2026-09-10 부터** 쓰기 시작했다.
